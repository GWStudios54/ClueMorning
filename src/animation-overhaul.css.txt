/* Deep Cut canvas preview. Worker-bundled and inlined only for ?motion-preview=deepcut. */
body.cm-deepcut-focus{
  background:var(--paper);
}
body.cm-deepcut-focus .masthead,
body.cm-deepcut-focus .tabs,
body.cm-deepcut-focus footer{
  display:none!important;
}
body.cm-deepcut-focus .app{
  width:min(920px,100%);
  margin:0 auto;
  padding-top:max(10px,env(safe-area-inset-top));
}
body.cm-deepcut-focus .panel:not(#deepcut){
  display:none!important;
}
.cm-deepcut-canvas-page.active{
  display:block;
  position:relative;
  isolation:isolate;
  margin:0 auto;
  padding:8px 12px 30px;
  contain:layout paint style;
}
.cm-focus-exit{
  appearance:none;
  border:0;
  background:transparent;
  color:var(--muted);
  font:700 13px/1.2 system-ui,sans-serif;
  padding:8px 4px 12px;
  cursor:pointer;
}
.cm-focus-exit:active{transform:translateY(1px)}
.cm-deepcut-canvas-page .panel-head{
  margin-top:0;
}
.cm-deepcut-stage{
  position:relative;
  width:100%;
  height:clamp(270px,42vw,350px);
  margin:14px auto 18px;
  overflow:hidden;
  border-radius:24px;
  background:#26221d;
  border:1px solid color-mix(in srgb,var(--line) 72%,#5b4932);
  contain:strict;
  isolation:isolate;
}
.cm-deepcut-canvas{
  display:block;
  width:100%;
  height:100%;
  touch-action:none;
}
.cm-canvas-hud{
  position:absolute;
  inset:12px 12px auto;
  display:flex;
  justify-content:space-between;
  gap:12px;
  pointer-events:none;
  color:#f5ead5;
  font:800 10px/1.1 system-ui,sans-serif;
  letter-spacing:.12em;
  text-shadow:0 1px 1px rgba(0,0,0,.5);
}
.cm-canvas-hud span{
  display:inline-flex;
  align-items:center;
  min-height:26px;
  padding:0 9px;
  border-radius:999px;
  background:rgba(19,17,15,.62);
  border:1px solid rgba(232,204,151,.16);
  backdrop-filter:none;
}
.cm-depth-readout strong{
  margin-left:5px;
  color:#e8c986;
}
.cm-scene-state{color:#e8c986}
.cm-deepcut-canvas-page .stats-row{
  position:relative;
  z-index:2;
}
.cm-deepcut-canvas-page .deepcut-prompt-card,
.cm-deepcut-canvas-page .guess-form,
.cm-deepcut-canvas-page .message,
.cm-deepcut-canvas-page .deepcut-history,
.cm-deepcut-canvas-page .start-card{
  position:relative;
  z-index:2;
}
.cm-deepcut-canvas-page .deepcut-prompt-card{
  box-shadow:none!important;
  transition:transform .16s ease,opacity .16s ease;
}
.cm-deepcut-canvas-page .guess-form{
  box-shadow:none!important;
}
.cm-deepcut-canvas-page.cm-canvas-urgent #deepCutTimer{
  color:var(--danger);
}
.cm-deepcut-canvas-page.cm-canvas-urgent .cm-scene-state{
  color:#f09b86;
}
.cm-deepcut-canvas-page.cm-canvas-complete .cm-deepcut-stage{
  border-color:color-mix(in srgb,var(--gold) 42%,var(--line));
}
@media(max-width:760px){
  body.cm-deepcut-focus .app{padding-left:8px;padding-right:8px}
  .cm-deepcut-canvas-page.active{padding:4px 4px 24px}
  .cm-deepcut-stage{
    height:300px;
    margin:10px auto 14px;
    border-radius:20px;
  }
  .cm-canvas-hud{
    inset:9px 9px auto;
    gap:6px;
    font-size:9px;
  }
  .cm-canvas-hud span{min-height:24px;padding:0 7px}
}
@media(max-width:420px){
  .cm-deepcut-stage{height:276px}
  .cm-depth-readout{letter-spacing:.08em}
}
@media(prefers-reduced-motion:reduce){
  .cm-deepcut-canvas-page .deepcut-prompt-card{transition:none}
}
